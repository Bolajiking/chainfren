/** @type {import('next').NextConfig} */
const nextConfig = {}
 output: 'export',

module.exports = {
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'images.ctfassets.net',
          port: '',
          pathname: '/3e17eg8a93v6/**',
        },
      ],

    },}
